plugins {
    id 'java' //
}

repositories {
    mavenCentral()
}

tasks.withType(JavaCompile) {
    sourceCompatibility = JavaVersion.VERSION_21 // версию джава выбираем ту, которую используете
    targetCompatibility = JavaVersion.VERSION_21
}